/**
 * Classes responsible for the "mock" feature of agent. These might be very
 * similar to classes already available in test classes of bulldog-core, but
 * with more focus on returning some random data.
 */
package board.test;
