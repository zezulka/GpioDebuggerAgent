/**
 * GPIO interface does not have to be dealt with since this is requied
 * by UX itself already. Moreover, these classes should only wrap around
 * already existing simple Write XOR Read requests.
 */
package request.writeread;
